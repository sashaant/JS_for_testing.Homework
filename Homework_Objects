1. Create object "post" which should contain
title
body
author
views
comment which contain information about the authot first and last name and comment body
isLive
someFunction: result of calling console.log()



then get comment body and someFunction values using dots and bracket notation

1.1 get the length of post object and  write the script which should return the list the properties values of a Post object


//RESULT

const post = {
    title: "Title 1",
    body: "Some big body",
    author: "John Doe",
    views: 995,
    comment: {
        commentAuthorFirstName: "Bob",
        commentAuthorLastName: "Qwerty", 
        commentBody: "Some comment body",
    },
    isLive: true,
    someFunction: function () {
        console.log("Hello")
    },
    someArrowFunction: () => console.log("Hello"),
}


console.log(post.comment)
console.log(post["comment"]["comment Body"]);
post.someFunction()


//1.1 get the length of post object and  write the script which should return the list the properties values of a Post object
//RESULTS

//console.log(Object.keys(post).length)
//console.log(Object.values(post).length)
//console.log(Object.getOwnPropertyNames(post).length)



//
for (let key in post){
    console.log(post[key])
}

